<!DOCTYPE html>
<html>
  <head>
   <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{title}}</title>
    {{!-- We will create separate css files for each page, which can be sent from nodejs --}}
    {{#each styles}}
    	<link rel="stylesheet" href="/{{this}}.css" media="screen" title="no title">
    {{/each}}

    {{#each scripts}}
      <script src="/{{this}}.js"></script>
    {{/each}}
   
  </head>
  	<body>
